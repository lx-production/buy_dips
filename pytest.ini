[pytest]
pythonpath = .
# Web3 7.16 imports its supported legacy WebSocket provider even though this bot uses HTTPProvider only.
filterwarnings =
    ignore:^websockets\.legacy is deprecated;.*:DeprecationWarning:websockets\.legacy
